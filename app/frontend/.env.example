# Base URL of Module 5 (app/backend)'s API. Matches app/backend/config.py's
# HOST=127.0.0.1 / PORT=8000 defaults. Copy this file to .env.local (gitignored)
# and change it only if your local backend runs on a different port.
VITE_API_BASE=http://127.0.0.1:8000
