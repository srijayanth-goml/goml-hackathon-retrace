# Shared dependencies across ReTrace modules that run LOCALLY: data_pipeline,
# unlearning (Module 3), verification (Module 4), and app/backend (Module 5) all run
# on a laptop/local machine per ../CLAUDE.md's architecture decision -- only Module 2
# (baseline fine-tuning) is Colab-only, so its EXTRA deps (trl, bitsandbytes,
# datasets -- see finetuning/requirements.txt) stay in that module's own file rather
# than here.
#
# torch/transformers/peft/accelerate/safetensors are included here (not in a separate
# unlearning/requirements.txt) because Module 3 needs them to run locally at all --
# consolidating into one root file is simpler than a per-module split for the modules
# that all run on the same local machine (locked decision, see plan.md's Module 3
# "Open decisions"). `pip install -r requirements.txt` from the repo root is enough
# to run data_pipeline AND unlearning; only Colab needs the extra
# finetuning/requirements.txt on top.
pandas>=2.0
pytest>=7.4
torch>=2.1
transformers>=4.44
peft>=0.11
accelerate>=0.33
safetensors>=0.4
sentencepiece>=0.2
# App backend (Module 5) -- runs locally like data_pipeline/unlearning/verification,
# so it shares this root file too rather than starting a separate
# app/backend/requirements.txt (locked decision, see plan.md's Module 5 Open
# Decisions: continuing the pattern the three prior local modules already established).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27          # needed by fastapi.testclient.TestClient (app/backend/tests/)
